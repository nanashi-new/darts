# Task: Убрать ЕВСК/EBCK Из Продукта И Расчетов

## Цель

Удалить зависимость продукта от ЕВСК/EBCK: из текстов, UI, документации и расчетной логики.

## Что Меняем

- Убрать UI настроек `norms.xlsx`, нормативов, разрядов и версии нормативов.
- Убрать загрузку нормативов из import/recalculate flow.
- Не считать разрядные бонусы: `points_classification = 0`.
- Считать рейтинг на основе турнирных очков/мест без ЕВСК-разрядов.
- Обновить README, requirements, architecture/rules docs и FAQ.
- Обновить диагностику: отсутствие `norms.xlsx` больше не ошибка.

## Что Не Трогаем

- Старые nullable DB-поля `rank_set`, `rank_sector20`, `rank_big_round` пока не удалять.
- Исторические release artifacts в `docs/artifacts/` не переписывать.
- Рейтинг и snapshots как концепцию не удалять.

## Тесты И Проверки

- Обновить тесты import/recalculate под отсутствие нормативов.
- Добавить тест, что missing `norms.xlsx` не блокирует диагностику.
- Добавить тест, что `points_classification` остается `0`.
- `rg` по `ЕВСК|EBCK|EVSK|norms.xlsx|разряд|норматив` и вручную разделить допустимые внутренние остатки от запрещенных видимых.

## Готово, Если

- Пользователь не видит ЕВСК/EBCK/нормативы/разряды.
- Импорт и пересчет работают без `norms.xlsx`.
- Релизные smoke-проверки не ожидают materialize `norms.xlsx`.
- Статус: выполнено в ветке `feature/darts-liga-no-evsk`.
