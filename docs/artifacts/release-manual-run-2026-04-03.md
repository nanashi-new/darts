# Отчёт ручного прогона релиза — 2026-04-03

## Метаданные
- Дата/время прогона (UTC): 2026-04-03 10:25 UTC
- Ветка / commit: `work` / `d130bf5` (pre-squash SHA прогона; артефакты ниже собраны до squash и привязаны к этому commit)
- Исполнитель: release-duty
- Окружение (OS, Python, сборка): Linux container, Python 3.12, local run
- Ссылка на PR: `release-task-2026-04-03` (стабильный ID задачи релиза)
- Ссылки на CI-run / артефакты: [`docs/artifacts/release-check-smoke-2026-04-03.log`](release-check-smoke-2026-04-03.log), [`docs/artifacts/release-run-2026-03-01_1640UTC.txt`](release-run-2026-03-01_1640UTC.txt)

## Обязательные сценарии

### 1) Import
- Что запускалось: не запускалось в этом прогоне
- Результат (PASS/FAIL): FAIL
- Ссылка(и) на подтверждение (лог, скриншот, файл): —
- Комментарий: блокер релиза; ручной импорт XLSX не подтверждён

### 2) Recalc
- Что запускалось: не запускалось в этом прогоне
- Результат (PASS/FAIL): FAIL
- Ссылка(и) на подтверждение (лог, скриншот, файл): —
- Комментарий: блокер релиза; ручной пересчёт не подтверждён

### 3) Export
- Что запускалось: `pytest -q -rs -m release_smoke` (headless smoke)
- Результат (PASS/FAIL): FAIL
- Ссылка(и) на подтверждение (лог, скриншот, файл): [`docs/artifacts/release-check-smoke-2026-04-03.log`](release-check-smoke-2026-04-03.log)
- Комментарий: smoke не завершён успешно (SKIPPED по `libGL.so.1`)

### 4) Merge
- Что запускалось: не запускалось в этом прогоне
- Результат (PASS/FAIL): FAIL
- Ссылка(и) на подтверждение (лог, скриншот, файл): —
- Комментарий: блокер релиза; ручной merge-сценарий не подтверждён

### 5) Audit
- Что запускалось: не запускалось в этом прогоне
- Результат (PASS/FAIL): FAIL
- Ссылка(и) на подтверждение (лог, скриншот, файл): —
- Комментарий: блокер релиза; аудит-сценарий не подтверждён

## Итог
- Общий статус ручного прогона: NOT READY
- Блокеры:
  - `release_smoke` не пройден полностью: `SKIPPED` из-за отсутствия `libGL.so.1` ([`docs/artifacts/release-check-smoke-2026-04-03.log`](release-check-smoke-2026-04-03.log)).
  - Не закрыты обязательные ручные сценарии import/recalc/export/merge/audit.
  - Не подтверждены сборка `.exe` и запуск на чистом ПК без Python.
- Риски:
  - Возможна деградация export/UI в целевом окружении без валидного smoke-прогона.
  - Возможны регрессии бизнес-сценариев без ручного подтверждения обязательных разделов.

## Правило закрытия релизного PR
Релизный PR/релиз **запрещено закрывать** до тех пор, пока:
1. не приложен заполненный отчёт ручного прогона по этому шаблону;
2. в каждом обязательном разделе (import/recalc/export/merge/audit) не добавлены ссылки на результаты.
