# Шаблон отчёта ручного прогона релиза

> Используйте этот шаблон для каждого релизного PR.
> Рекомендуемое имя файла: `docs/artifacts/release-manual-run-YYYY-MM-DD.md`.

## Метаданные
- Дата/время прогона (UTC):
- Ветка / commit:
- Исполнитель:
- Окружение (OS, Python, сборка):
- Ссылка на PR:
- Ссылки на CI-run / артефакты:

## Обязательные сценарии

### 1) Import
- Что запускалось:
- Результат (PASS/FAIL):
- Ссылка(и) на подтверждение (лог, скриншот, файл):
- Комментарий:

### 2) Recalc
- Что запускалось:
- Результат (PASS/FAIL):
- Ссылка(и) на подтверждение (лог, скриншот, файл):
- Комментарий:

### 3) Export
- Что запускалось:
- Результат (PASS/FAIL):
- Ссылка(и) на подтверждение (лог, скриншот, файл):
- Комментарий:

### 4) Merge
- Что запускалось:
- Результат (PASS/FAIL):
- Ссылка(и) на подтверждение (лог, скриншот, файл):
- Комментарий:

### 5) Audit
- Что запускалось:
- Результат (PASS/FAIL):
- Ссылка(и) на подтверждение (лог, скриншот, файл):
- Комментарий:

## Итог
- Общий статус ручного прогона: READY / NOT READY
- Блокеры:
- Риски:

## Правило закрытия релизного PR
Релизный PR/релиз **запрещено закрывать** до тех пор, пока:
1. не приложен заполненный отчёт ручного прогона по этому шаблону;
2. в каждом обязательном разделе (import/recalc/export/merge/audit) не добавлены ссылки на результаты.
