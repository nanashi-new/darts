[pytest]
markers =
    unit: Fast unit tests for pure business logic.
    integration: Integration tests across services, DB, and filesystem.
    release_smoke: Minimal end-to-end smoke tests for release validation.
